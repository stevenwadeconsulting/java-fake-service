version: "3.7"
services:
  webapp:
    build: .
    ports:
      - "8080:8080"

#version: "3.7"
#services:
#  webapp:
#    build: .
#    ports:
#      - "8080:8080"
#    environment:
#      - training_name=123
#      - training_step=3

#version: "3.7"
#services:
#  webapp:
#    build: .
#    ports:
#      - "8080:8080"
#    environment:
#      - training_name=123
#      - training_step=3
#    volumes:
#      - type: bind
#        source: ./properties.txt
#        target: /config/application.properties